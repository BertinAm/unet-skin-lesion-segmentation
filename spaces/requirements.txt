# Inference-only dependencies for the Hugging Face Space.
# (No training/experiment tooling: wandb, kaggle, jupytext, matplotlib are omitted.)
# Gradio itself is provided by the Space SDK (see README.md sdk_version) — do not pin it here.

--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.1.0
torchvision>=0.16.0

segmentation-models-pytorch>=0.3.3
timm>=0.9.2

albumentations>=1.3.1
opencv-python-headless>=4.8.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
scipy>=1.11.0
pillow>=10.0.0
pyyaml>=6.0

grad-cam>=1.5.0
huggingface_hub>=0.20.0

# Python 3.13 removed the stdlib `audioop` module that gradio->pydub imports.
# This backport restores it so the app starts on HF's Python 3.13 runtime.
audioop-lts; python_version >= "3.13"
